name: Test

on: [push]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.10
    - name: Install dependencies
      run: |
        C:\Miniconda\condabin\conda.bat env create -f envs\environment_test.yml
        C:\Miniconda\condabin\conda.bat init powershell
    - name: Test
      run: |
        # Activate the test environment
        C:\Miniconda\condabin\conda.bat activate test
        C:\Miniconda\condabin\conda.bat install pytest pytest-black pytest-flakes
        pip install -e .
        pytest tests/
